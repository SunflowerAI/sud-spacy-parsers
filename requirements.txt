spacy>=3.8,<3.9
click>=8.0  # spaCy imports click directly; newer typer no longer pulls it in
thinc-apple-ops; platform_machine == "arm64"

# Korean. natto-py + Homebrew mecab-ko is what this machine has (MECAB_PATH); python-mecab-ko is the
# pip route that vendors both the library and mecab-ko-dic, and is what a ko wheel carrying
# `sud.KoAnalyserEmbed.v1` would have to declare. scripts/ko_analyser.py takes whichever is present
# and refuses rather than parsing on with an unanalysed channel.
# python-mecab-ko  # uncomment for the analyser channel without Homebrew
